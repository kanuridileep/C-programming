//C program to print factors of a given number
#include <stdio.h>
int main()
{
	int n,i=2;
	printf("enter a number to find factors:");
	scanf("%d",&n);
	printf("1\n");
	while (i<=n/2)
	{
		if (n%i==0)
		printf("%d\n",i);
	    	i++;
	}
	printf("%d",n);
	return 0;
}
